# 攻击路径分析汇总

## 结论
- `f01-unsigned-windows-update-chain`：若攻击者能替换 GitHub Releases 上的元数据与安装包，即可在用户确认更新后执行任意代码。
- `f02-workspace-artifact-packaging-leak`：若开发者在脏工作树中本地打包或使用辅助脚本推送，内部扫描与临时目录内容可能对外暴露。

## 排序依据
1. 先按可利用后的直接影响排序：任意代码执行高于信息泄漏。
2. 再按触发前提排序：发布通道被攻陷虽有前提，但一旦成立影响极高；产物泄漏更容易触发，但影响主要是信息暴露。
